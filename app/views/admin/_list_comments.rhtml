<h4>Comments(<%= @comments.size %>)</h4>
  <table style="width:80%" class="admin_comment_box" style="margin:10px">
<% for @comment in @comments %>
 <tr class="<%= cycle("admin_row_a", "admin_row_b") %> ">
    <td><%= @comment.name %></td>
    <td><%= h @comment.ip %></td>
    <td style="width:200px" align=center>   <%= h truncate(@comment.comment) %></td>
    <td><%= "#{truncate(@comment.created_at.strftime("%m/%d/%y"))}" %></td>
    <td style="width:20px">

   <%= link_to image_tag("admin/icon_destroy.png", :title => "destroy", :alt => "Destroy", :border => 0),
       {:action => "destroy_comment", :id => @comment.id, :post_id => @post.id }, 
       :confirm => "Are you sure you want to delete this comment?"
   %>

    </td>
   </tr>
<% end %>
</table>
